# R-06 Webpack Consumer

Минимальный consumer проверяет production-сборку Rovna UI через Webpack и физически установленные release tarballs. Он использует `RovnaUI`, `Button` и `SMaterials`; JSX-loader не требуется.

Исполняемый маршрут: `npm.cmd run bundlers:r06` из `app/`.
