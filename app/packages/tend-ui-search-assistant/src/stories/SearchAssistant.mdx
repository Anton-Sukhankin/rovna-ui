import { Meta } from '@storybook/addon-docs/blocks';

import Img1 from './1.png';
import Img2 from './2.png';
import Img3 from './3.png';

<Meta title='Rovna UI/Search Assistant/Search Assistant (Умный поиск)' />

# Search Assistant (Умный поиск)

## Установка

```sh
yarn add @rovna-ui/search-assistant@latest
```

## Использование

#### На проекте импортируем и используем компонент по месту:

```javascript
import { SearchAssistant } from '@rovna-ui/search-assistant';

export const App = () => (
  <Layout>
    <SearchAssistant />
  </Layout>
);
```

<img src={Img1} alt='Standart Using' />
<br />
<br />

#### Использование через свою иконку или кастомный элемент для открытия чата:

```javascript
import { SearchAssistant } from '@rovna-ui/search-assistant';

export const App = () => (
  <Layout>
    <SearchAssistant
      renderEntry={handleOpen => <Button onClick={handleOpen}>Кастомная кнопка</Button>}
    />
  </Layout>
);
```

<img src={Img2} alt='Custom Render Entry' />
<br />
<br />

#### Использование на статичной странице, контейнере или webview (без функционала открытия):

```javascript
import { SearchAssistant } from '@rovna-ui/search-assistant';

export const App = () => (
  <Layout>
    <SearchAssistant chatOnly />
  </Layout>
);
```

<img src={Img3} alt='Chat Only' />
