import { Primary, Controls, Meta, Stories } from '@storybook/addon-docs/blocks';

import * as BurgerMenuStories from './BurgerMenu.stories.tsx';

<Meta of={BurgerMenuStories} title='BurgerMenu' />

# BurgerMenu

## Использование

> Компонент используется только в рамка компонента `Header`.
> Команда дизайн-системы не несет ответственность за директивное импортирование и использование компонента вне публичного API.

```tsx
import { Header } from '@rovna-ui/header';

<Header burger={{ items: [], selectedKeys: [], onSelect: function () {} }} />;
```

<Primary />
<Controls />
<Stories />
