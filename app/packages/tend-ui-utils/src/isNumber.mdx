import { Meta } from '@storybook/addon-docs/blocks';

<Meta title='Rovna UI/Utils/isNumber' />

# isNumber

## Использование

```tsx
import { isNumber } from '@rovna-ui/utils';
```

### Для лучшего `tree-shaking`

```tsx
import { isNumber } from '@rovna-ui/utils/isNumber';
```

```js
console.log(isNumber('Hello World')); // false
console.log(isNumber(() => {})); // false
console.log(isNumber({})); // false
console.log(isNumber([])); // false
console.log(isNumber(null)); // false
console.log(isNumber(undefined)); // false
console.log(isNumber(true)); // false
console.log(isNumber(false)); // false

console.log(isNumber(1)); // true
```
