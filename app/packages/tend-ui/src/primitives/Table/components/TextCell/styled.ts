import { INTERNAL_TypographyBase } from '@rovna-ui/typography';
import styled from 'styled-components';

export const Root = styled(INTERNAL_TypographyBase)`
  color: inherit;
  font-size: inherit;
  line-height: inherit;
`;
