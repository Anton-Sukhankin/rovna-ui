import styled from 'styled-components';

import { Button as _Button } from '@rovna-internal/components/primitives/Button';

export const Button = styled(_Button)`
  margin-right: auto;
` as typeof _Button;
