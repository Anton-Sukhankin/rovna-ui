import { Meta } from '@storybook/addon-docs/blocks';

import { Table } from '@rovna-ui/components/primitives';
import { RovnaUI } from '@rovna-ui/components/theme';
import { Tag } from '@rovna-ui/primitives';
import { Text } from '@rovna-ui/typography';

import * as tokens from '@packages/tend-ui-tokens/package.json';
import * as factories from '@packages/tend-ui-factories/package.json';
import * as utils from '@packages/tend-ui-utils/package.json';
import * as types from '@packages/tend-ui-types/package.json';
import * as hooks from '@packages/tend-ui-hooks/package.json';
import * as fonts from '@packages/tend-ui-fonts/package.json';
import * as typography from '@packages/tend-ui-typography/package.json';
import * as logos from '@packages/tend-ui-logos/package.json';
import * as icons from '@packages/tend-ui-icons/package.json';
import * as favicons from '@packages/tend-ui-favicons/package.json';
import * as grid from '@packages/tend-ui-grid/package.json';
import * as primitives from '@packages/tend-ui-primitives/package.json';
import * as main from '@packages/tend-ui/package.json';
import * as filters from '@packages/tend-ui-filters/package.json';
import * as table from '@packages/tend-ui-table/package.json';
import * as header from '@packages/tend-ui-header/package.json';
import * as notifications from '@packages/tend-ui-notifications/package.json';
import * as search from '@packages/tend-ui-search-assistant/package.json';
import * as tree from '@packages/tend-ui-tree/package.json';
import * as columnsSettings from '@packages/tend-ui-columns-settings/package.json';
import * as upload from '@packages/tend-ui-upload/package.json';
import * as form from '@packages/tend-ui-form/package.json';

<Meta title='Общая информация/Актуальные версии пакетов' />

# Актуальные версии пакетов

<RovnaUI>
  <Table
    columns={[
      { title: 'Название', dataIndex: 'name' },
      {
        title: 'Версия',
        dataIndex: 'version',
        render: value =>
          value === '0.0.0' ? (
            <Tag preset='red'>Не выпущен</Tag>
          ) : (
            <Tag preset='green'>{value}</Tag>
          ),
      },
      {
        title: 'Описание',
        dataIndex: 'description',
      },
      {
        title: 'Changelog',
        dataIndex: 'changelog',
        render: value => (
          <a target='_blank' href={value}>
            Перейти
          </a>
        ),
      },
    ]}
    dataSource={[
      tokens,
      factories,
      utils,
      types,
      hooks,
      fonts,
      typography,
      icons,
      favicons,
      logos,
      grid,
      primitives,
      main,
      filters,
      table,
      header,
      notifications,
      search,
      tree,
      columnsSettings,
      upload,
      form,
    ].map(item => ({
      version: item.version,
      name: item.name,
      description: item.description || '-',
      changelog: `/docs/${item.name.replace('@rovna-ui/', '')}-changelog--docs`,
    }))}
  />
</RovnaUI>
