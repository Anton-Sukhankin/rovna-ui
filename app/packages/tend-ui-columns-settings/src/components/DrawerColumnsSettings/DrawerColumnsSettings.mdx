import { Primary, Controls, Meta, Stories } from '@storybook/addon-docs/blocks';
import { Status } from '@rovna-internal/components/tools/Status';

import * as DrawerColumnsSettingsStories from './DrawerColumnsSettings.stories.tsx';

<Meta of={DrawerColumnsSettingsStories} title='DrawerColumnsSettings' />
<Status status='ready' />

# DrawerColumnsSettings

## Использование

```tsx
import { DrawerColumnsSettings, useColumns } from '@rovna-ui/columns-settings';
import { Table } from '@rovna-ui/components/primitives';

const settings = useColumns({
  defaultColumns: [
    { id: '1', label: 'Label 1' },
    { id: '2', label: 'Label 2' },
    { id: '3', label: 'Label 3' },
  ],
  onColumnsChange: () => {}
});

<Table
  columns={settings.getAntdTableColumns()}
/>
<DrawerColumnsSettings
  open={...}
  onClose={...}
  settings={settings}
/>;
```

<Primary />
<Controls />
<Stories />
